// Arithmetic operators
// Sum: a + b | a.plus(b) | a += b
// Subtraction: a - b | a.minus(b) | a -= b
// Multiplication: a * b | a.times(b) | a *= b
// Division: a / b | a.div(b) | a /= b

fun main(){
    var count = "Hello, "
    var times = "world!"
    
    println(count + times)
}
